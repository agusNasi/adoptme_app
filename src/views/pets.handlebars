<div class='container'>
  <ul class='collection'>
    {{#each pets}}
      <li class='collection-item avatar'>
        <img src={{this.image}} alt='pet.png' class='circle' />
        <span class='title'>{{this.name}}</span>
        <p>{{this.specie}}</p>
      </li>
    {{/each}}
  </ul>
</div>